name: "Joke Action"
description: "Fetches a random joke and exposes it as an output"

outputs:
  joke:
    description: "The fetched joke text"

runs:
  using: node24
  main: dist/index.jsgit add action.yml
git commit -m "Add action metadata file"
git push